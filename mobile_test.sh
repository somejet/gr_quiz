#!/bin/bash
# Скрипт для тестирования на мобильном устройстве через локальную сеть

echo "🌐 Запуск локального сервера для мобильного тестирования..."

# Получаем IP адрес
IP=$(ifconfig | grep "inet " | grep -v 127.0.0.1 | awk '{print $2}' | head -1)

echo "📱 Ваш IP адрес: $IP"
echo "🔗 Откройте на мобильном устройстве: http://$IP:8080"
echo "📱 Убедитесь, что мобильное устройство подключено к той же Wi-Fi сети"

# Запускаем Flutter веб-сервер
flutter run -d chrome --web-port 8080 --web-hostname 0.0.0.0
